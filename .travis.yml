language: go
dist: precise
go:
  - 1.8.3
  # XXX temporarily disabled, because tip produces inconsistent go fmt results
  #- tip
install:
  # Rename TheTorProject to all lowercase to make imports work properly
  - mv $GOPATH/src/github.com/TheTorProject $GOPATH/src/github.com/thetorproject
  - cd $GOPATH/src/github.com/thetorproject/proteus
  # Install govendor
  - make vendor
script:
  - make check
